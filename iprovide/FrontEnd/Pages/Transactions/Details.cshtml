@page
@model FrontEnd.Pages.Transactions.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Transaction</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Transaction.Amount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Transaction.Amount)
        </dd>
        <dt class="col-sm-2">
            Debt after this transaction
        </dt>
		<dd class="col-sm-10">
			@((Model.Transaction.PersonId.ToString().Equals(User.Claims.FirstOrDefault(x => x.Type == "PersonId").Value)) ? (Model.Transaction.PayerDebtBalance) : (Math.Abs(Model.Transaction.PayerDebtBalance)))
		</dd>
        @*<dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Transaction.PayerBillsBalance)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Transaction.PayerBillsBalance)
        </dd>*@
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Transaction.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Transaction.Date)
        </dd>
    </dl>
</div>
<div>
    @*<a asp-page="./Edit" asp-route-id="@Model.Transaction.Id">Edit</a> |*@
    <a asp-page="./Index">Back to List</a>
</div>
